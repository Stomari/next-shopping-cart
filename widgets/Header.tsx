import styles from '../styles/Header.module.scss';

const Header = () => {
  return (
    <div className={styles.container}>
      <h3 className={styles.title}>Next Shopping Cart</h3>
    </div>
  )
};

export default Header;